
# Ignore any hg stuff in the patch queue
mozilla/patches/.hg
mozilla/patches/.hgignore
mozilla/patches/.mq
mozilla/patches/status
mozilla/patches/guards

# Junk that could exist anywhere:
.DS_Store
*.swp
*.tmp
.*.gz

# A handy place to put stuff that git should ignore:
/ignore

# Build artifacts
/built

# Editor junk
/.project
/.pydevproject
/.settings/
/.settings.xml
/.settings.xml.old

# Manually installed node packages should be ignored
/node_modules/
/.externalToolBuilders
